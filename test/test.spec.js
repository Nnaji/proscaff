const expect = require('chai').expect;
describe('Main Project', () => {
  describe('Truthy Value', () => {
    it('should test for equality that returns true', () => {
      expect(1).to.be.equal(1);
    });
  });
});
